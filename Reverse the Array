#include <bits/stdc++.h>

using namespace std;

int main()
{
  int a[10]={1,2,4,6,8,2,29,7,8,9};
  int n=10;
  int start=0;
  int end=n-1;
  while(start<end)
  {
      swap(a[start],a[end]);
      start++;
      end--;
  }
  for(int i=0;i<n;i++)
  {
      cout<<a[i]<<" ";
  }
    return 0;
}
